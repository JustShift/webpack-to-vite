# Open work items

Tracked items that are scoped, understood, and not yet implemented. Each one represents a known gap between what a user might reasonably expect and what the analyzer does today.

When implementing one of these, drop the entry from this file. The 2026-06-11 audit found 11 bugs (verified against the built CLI); all are fixed and covered by tests. The 2026-06-12 pass implemented all eight tracked improvements (build.lib emission, nested-key unmapped warnings, css.preprocessorOptions emission, entry static-eval, the index.html skeleton, devServer.static → publicDir, the real-project fixture suite, and the opt-in `--apply --deps` package.json update).

## Open

*(none — file new items here as they are scoped)*
